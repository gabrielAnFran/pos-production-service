apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "production-service.fullname" . }}-worker
  labels:
    {{- include "production-service.labels" . | nindent 4 }}
    app.kubernetes.io/component: worker
spec:
  replicas: {{ .Values.worker.replicaCount }}
  selector:
    matchLabels:
      {{- include "production-service.labels" . | nindent 6 }}
      app.kubernetes.io/component: worker
  template:
    metadata:
      labels:
        {{- include "production-service.labels" . | nindent 8 }}
        app.kubernetes.io/component: worker
    spec:
      containers:
        - name: worker
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}-worker"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ include "production-service.fullname" . }}-config
            - secretRef:
                name: {{ include "production-service.fullname" . }}-secret
          resources:
            {{- toYaml .Values.worker.resources | nindent 12 }}
